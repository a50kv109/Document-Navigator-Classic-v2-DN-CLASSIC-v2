[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dn-classic"
version = "0.1.0"
description = "DN CLASSIC Monorepo"
dependencies = []

[tool.pytest.ini_options]
pythonpath = [
    "core/src",
    "pipeline/src"
]
testpaths = ["tests"]
